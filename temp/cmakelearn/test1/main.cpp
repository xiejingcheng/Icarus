#include <stdio.h>

int main()
{
    int a = 200;
    int b = 12;
    printf("a = %d, b = %d中文\n", a, b);
    // printf("a + b = %d\n", add(a, b));
    // printf("a - b = %d\n", subtract(a, b));
    // printf("a * b = %d\n", multiply(a, b));
    // printf("a / b = %f\n", divide(a, b));
 
    return 0;
}